# UniFi UDM Pro Configuration
# Copy this file to .env and fill in your credentials

# UniFi Controller Settings
UNIFI_HOST=192.168.53.1
UNIFI_USERNAME=your_local_admin_username
UNIFI_SITE=default
UNIFI_VERIFY_SSL=false

# Password Configuration
# IMPORTANT: For security, passwords are stored encrypted by default.
# 
# Option 1 (RECOMMENDED): Use encrypted password storage
#   1. Leave UNIFI_PASSWORD commented out or unset
#   2. Run: python3 password_manager.py encrypt "your_password_here"
#   3. The encrypted password will be stored in .env.password.encrypted
#   4. The encrypted file is automatically ignored by git
#
# Option 2: Use plaintext password (less secure, not recommended)
#   UNIFI_PASSWORD=your_plaintext_password
#
# The code will automatically use the encrypted password if UNIFI_PASSWORD is not set.
# UNIFI_PASSWORD is stored encrypted in .env.password.encrypted

# Notes:
# - Use a LOCAL administrator account (not cloud/SSO) for full API access
# - The .env and .env.password.encrypted files are in .gitignore and will not be committed
# - To update your encrypted password, run: python3 password_manager.py encrypt "new_password"
